# Working renders — regenerate with `python render_panel.py --sample`.
# The two demo images the README embeds live in docs/ and are committed.
out/

# Secrets: the HA long-lived token is read from the environment, never committed.
.env
secrets.yaml

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

.vscode/
.idea/
Thumbs.db
desktop.ini
